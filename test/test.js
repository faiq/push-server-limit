var assert = require("assert")
  , middleware = require("../lib/middleware.js")

describe("the middleware", function (done) {
  it ("should make default options, when nothing is passed in", function (done) { 
    done()    
  })
})
